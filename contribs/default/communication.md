# Communication defaults

Prefer concise, direct recommendations. State tradeoffs only when they affect the decision. Ask before durable context changes.

This public default contains no personal data. Users may exclude it during `holoself init --exclude-contrib communication` or select explicit contribs with `--contribs`.
